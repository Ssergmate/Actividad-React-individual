# Situación de Aprendizaje - SPA con React

Este proyecto consiste en el desarrollo de una Single Page Application (SPA) utilizando **React** y **Vite**. La aplicación simula un entorno de gestión escolar aplicando conceptos de routing, autenticación global, carga perezosa (Lazy Loading) y consumo de APIs externas.

## 🚀 Instrucciones de Ejecución

Sigue estos pasos para visualizar el proyecto en tu máquina local:

1. **Instalar dependencias:**
   Abre una terminal en la carpeta del proyecto y ejecuta:

   npm install
   
3. Arrancar el servidor de desarrollo: Una vez instaladas las librerías, inicia la aplicación con:

npm run dev

3. Abrir en el navegador: Pulsa Ctrl + Clic en la URL que aparece en la terminal (normalmente http://localhost:5173).
